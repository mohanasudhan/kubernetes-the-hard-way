# imperative 
# kubectl create configmap \
#               app-config --from-literal=APP_COLOR=red or from file
apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
data:
  APP_COLOR: blue
  APP_MODE: prod
